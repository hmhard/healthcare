{% extends 'base.html.twig' %}

{% block title %}Edit Department{% endblock %}

{% block body %}
     <div class="col-xl-8 order-xl-1">
          <div class="card">
            <div class="card-header">
              <div class="row align-items-center">
                <div class="col-8">
                  <h3 class="mb-0">Department show </h3>
                </div>
                <div class="col-4 text-right">
                  <a href="{{path('department_index')}}" class="btn btn-sm btn-primary">Back</a>
                </div>
              </div>
            </div>
            <div class="card-body">


    {{ include('department/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('department_index') }}">back to list</a>

    {{ include('department/_delete_form.html.twig') }}
</div>
</div>
</div>
</div>
{% endblock %}
