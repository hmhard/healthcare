{% extends 'base.html.twig' %}

{% block title %}New UserType{% endblock %}

{% block body %}
     <div class="col-xl-8 order-xl-1">
          <div class="card">
            <div class="card-header">
              <div class="row align-items-center">
                <div class="col-8">
                  <h3 class="mb-0">Feed required requirements </h3>
                </div>
                <div class="col-4 text-right">
                  <a href="{{path('user_type_index')}}" class="btn btn-sm btn-primary">Back</a>
                </div>
              </div>
            </div>
            <div class="card-body">
         

    {{ include('user_type/_form.html.twig') }}

    <a href="{{ path('user_type_index') }}">back to list</a>
{% endblock %}
